import { html } from 'htm/preact'
import { FunctionComponent } from 'preact'

export const Create:FunctionComponent = function () {
    return html`<div class="route connect">
        <h2>Connect to an esisting identity</h2>
    </div>`
}
